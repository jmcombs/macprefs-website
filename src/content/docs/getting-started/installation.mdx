---
title: Installation
description: How to install macprefs on your Mac
---

# Installation

macprefs is distributed via Homebrew for easy installation on macOS.

## Prerequisites

- macOS 13 (Ventura) or later
- Homebrew package manager

## Install via Homebrew

```bash
brew tap jmcombs/macprefs
brew install macprefs
```

Or as a single command:

```bash
brew install jmcombs/macprefs/macprefs
```

## Verify Installation

After installation, verify macprefs is working:

```bash
macprefs --version
```

You should see the version number displayed.

## Next Steps

Now that macprefs is installed, head to the [Quick Start](/getting-started/quick-start/) guide to create your first configuration.
